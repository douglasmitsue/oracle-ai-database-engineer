Instalar o Docker Desktop: Verificar a documentação do docker
#### Subir o container do Oracle no Docker

docker run -d --name oracle --hostname oracledb -p 1521:1521 -e ORACLE_PWD=<escolha a sua senha> -e ORACLE_CHARACTERSET=AL32UTF8 -v oracle-26ai-data:/opt/oracle/oradata --restart unless-stopped container-registry.oracle.com/database/free:23.26.1.0